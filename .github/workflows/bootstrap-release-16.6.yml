name: Bootstrap BattleMender 16.6 release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staged:
    runs-on: ubuntu-latest
    steps:
      - run: echo "16.6 payload staging only"
